package com.example.ttms.config.spring;

/**
 * 旧版 Servlet 初始化入口已停用。
 * 当前项目统一通过 Spring Boot 主类 TtmsApplication 启动，
 * 保留此类仅用于说明迁移状态，避免作为重复启动入口参与编译和装配。
 */
@Deprecated
public final class WebInit {
    private WebInit() {
    }
}
