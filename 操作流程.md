# TTMS 票务管理系统 — 操作流程（当前项目版）

> 本文档基于当前项目源码、当前前端路由、当前后端接口权限、当前 `init.sql` 测试数据整理。
>
> 重点提醒：**文档以下内容描述的是“当前代码真实行为”**，不是理想设计行为。

---

## 0. 启动与访问

### 0.1 本地启动

#### 启动后端

项目根目录执行：

```bat
start-backend.cmd
```

或：

```bat
mvnw.cmd spring-boot:run
```

后端默认地址：

```text
http://localhost:8080
```

接口基础路径：

```text
/rest
```

#### 启动前端

```bash
cd TTMS_vue3_web
npm install
npm run dev
```

前端默认地址：

```text
http://localhost:5173
```

---

### 0.2 Docker 启动

项目根目录执行：

```bash
docker compose up -d --build
```

服务地址：

| 服务 | 地址 |
|------|------|
| 前端 | `http://localhost` |
| 后端 | `http://localhost:8080` |
| MySQL | `localhost:3306` |

数据库默认信息：

- 用户名：`root`
- 密码：`12345678`
- 数据库：`ttms`

> 如果你修改过 `init.sql`，但数据库数据没有变化，通常是因为旧 MySQL 数据卷仍在。此时要执行：
>
> ```bash
> docker compose down -v
> docker compose up -d --build
> ```

---

## 1. 当前登录账号

### 1.1 登录字段说明

登录时必须输入：

```text
用户名：employee.emp_name
密码：明文密码
```

不要输入 `emp_no`。

例如：

- 应输入 `admin`
- 不应输入 `A100`

---

### 1.2 当前 `init.sql` 内置账号

| 角色 | emp_type | 用户名 | 密码 | 用途 |
|------|:--:|------|------|------|
| 管理员 | 3 | `admin` | `admin123` | 用户管理、演出厅管理 |
| 经理 | 1 | `manager` | `123456` | 剧目、排片、座位、字典、销售分析 |
| 售票员 | 2 | `seller01` | `123456` | 未处理订单、前端退票页入口测试 |
| VIP | 4 | `vip01` | `123456` | 购票、退票流程测试 |
| 普通用户 | 5 | `user01` | `123456` | 普通购票测试 |

---

## 2. 当前菜单与权限速查

基于 `router/index.js` 和 `Layout.vue`，当前前端页面权限如下：

| 路由 | 页面 | 允许角色 |
|------|------|------|
| `/login` | 登录页 | 所有用户 |
| `/` | 首页 | 已登录用户 |
| `/profile` | 个人中心 | 已登录用户 |
| `/employees` | 用户管理 | 管理员 `3` |
| `/studios` | 演出厅管理 | 管理员 `3` |
| `/plays` | 剧目管理 | 经理 `1` |
| `/schedules` | 排片管理 | 经理 `1` |
| `/seats` | 座位管理 | 经理 `1` |
| `/dictionary` | 字典管理 | 经理 `1` |
| `/analysis` | 销售分析 | 经理 `1` |
| `/sell` | 购票 | VIP `4`、普通用户 `5` |
| `/return-ticket` | 退票 | 售票员 `2`、VIP `4` |
| `/unhandled` | 未处理订单 | 售票员 `2` |

### 2.1 当前前后端权限不一致说明

这里必须特别注意：

- 前端允许 `seller01` 进入 `/return-ticket`
- 但后端 `SaleRestController` 当前只允许：
  - VIP `4`
  - 普通用户 `5`

参与购票类订单操作

因此当前真实效果是：

| 账号 | 能否进入购票页 | 能否提交购票 | 能否进入退票页 | 能否提交退票流程 |
|------|------|------|------|------|
| `vip01` | 可以 | 可以 | 可以 | 可以 |
| `user01` | 可以 | 可以 | 不行 | 不行 |
| `seller01` | 不行 | 不行 | 可以 | **大概率会被后端拦截** |

如果你现在不再继续改后端，就按这个现实行为测试。

---

## 3. 当前初始化数据说明

当前 `init.sql` 已经换成新的测试数据，主要包括：

### 3.1 演出厅

| studio_id | 名称 | 说明 |
|------|------|------|
| 1 | 晨星厅 | 中小型标准影厅 |
| 2 | 海蓝厅 | 常规主力影厅 |
| 3 | 云幕VIP厅 | VIP 影厅 |

### 3.2 剧目

| play_id | 剧名 | 类型 | 语言 | 默认票价 |
|------|------|------|------|------|
| 1 | 迷城追踪 | 悬疑片 | 中文 | 49.00 |
| 2 | 月海计划 | 科幻片 | 中文字幕 | 69.00 |
| 3 | 周末训练营 | 喜剧片 | 中文 | 39.00 |
| 4 | 风筝小队 | 动画片 | 中文 | 45.00 |

### 3.3 数据字典

#### 剧目类型

- 悬疑片
- 科幻片
- 喜剧片
- 动画片

#### 语言

- 中文
- 英文
- 中文字幕

### 3.4 已内置的订单/票务状态

初始化后数据库里已经带有：

- 一部分已售票
- 一部分占座中票
- 一部分不可用座位

这样你打开页面后就能直接看到不同状态效果，不需要从纯空白数据开始搭。

---

## 4. 购票时间窗口规则

当前后端代码中的限制不是“所有未来场次都能买”，而是：

| 用户类型 | 可购买时间窗口 |
|----------|------|
| 普通用户 `emp_type=5` | 开演前 30 分钟内 |
| VIP `emp_type=4` | 开演前 50 分钟内 |

也就是说：

- 如果场次距离开演还很久，普通用户看得到剧目，也不一定能真正下单
- VIP 的可购买窗口比普通用户更早

### 4.1 为什么初始化后可能暂时买不了票？

因为 `init.sql` 的排片时间是固定晚间场次，用于：

- 初始化展示
- 票状态展示
- 排片/剧目/座位联动验证

如果你想“马上测试购票成功”，最稳妥的方法是：

1. 用 `manager / 123456` 登录
2. 进入 `/schedules`
3. 新建一个距离当前时间 **30 分钟内** 的场次
4. 再用 `user01` 或 `vip01` 去买

如果你想测试 VIP 提前购票，则把新场次设置在：

- 距当前时间 30～50 分钟内

这样：

- `vip01` 能买
- `user01` 不能买

---

## 5. 从零开始的推荐测试顺序

如果你使用的是已经导入新 `init.sql` 的数据库，建议这样测：

1. 管理员登录验证
2. 经理登录验证
3. 普通用户购票验证
4. VIP 提前购票验证
5. 售票员页面权限验证
6. 销售分析验证

下面给出具体步骤。

---

## 6. 管理员流程

### 6.1 登录管理员

账号：

```text
admin / admin123
```

可见菜单：

- 首页
- 用户管理
- 演出厅管理
- 个人中心

### 6.2 用户管理

路由：

```text
/employees
```

可以测试：

- 查看用户列表
- 搜索用户
- 新增用户
- 修改用户
- 删除用户

### 6.3 演出厅管理

路由：

```text
/studios
```

可以测试：

- 查看演出厅
- 新增演出厅
- 修改演出厅
- 删除演出厅

> 注意：创建演出厅后，不会自动在前端马上出现座位图，要由经理在座位管理中初始化。

---

## 7. 经理流程

### 7.1 登录经理

账号：

```text
manager / 123456
```

可见菜单：

- 剧目管理
- 排片管理
- 座位管理
- 字典管理
- 销售分析
- 个人中心

### 7.2 字典管理

路由：

```text
/dictionary
```

当前初始化数据已经有：

- 剧目类型
- 语言

这里主要用于验证：

- 字典是否能正常显示中文
- 是否还能新增/修改/删除字典项

### 7.3 剧目管理

路由：

```text
/plays
```

当前已有 4 个剧目，可直接验证：

- 中文名称是否正常显示
- 查询是否正常
- 新增是否正常
- 修改是否正常

### 7.4 演出厅座位管理

路由：

```text
/seats
```

用于：

- 查看影厅座位
- 初始化新影厅座位
- 修改座位可用/不可用状态

### 7.5 排片管理

路由：

```text
/schedules
```

可以做两类测试：

#### 测试 A：查看初始化排片

确认页面能正常显示：

- 剧目
- 影厅
- 时间
- 票价

#### 测试 B：新建可立即购票的排片

为了方便普通用户立刻测试购票，建议新建一个：

- 开演时间在当前时间 30 分钟内

为了测试 VIP 提前购票，建议新建一个：

- 开演时间距离现在 30～50 分钟

---

## 8. 普通用户购票流程

### 8.1 登录普通用户

```text
user01 / 123456
```

可见菜单：

- 首页
- 购票
- 个人中心

路由：

```text
/sell
```

### 8.2 购票步骤

```text
① 选择剧目
      ↓
② 选择可购排片
      ↓
③ 选择绿色可选座位
      ↓
④ 生成订单
      ↓
⑤ 完成加法验证
      ↓
⑥ 支付成功
```

### 8.3 座位颜色说明

| 颜色 | 状态 | 含义 |
|------|------|------|
| 绿色 | `ticket_status=0` | 可选 |
| 灰色 | `ticket_status=1` | 已售 |
| 黄色 | `ticket_status=2` | 占座中 |
| 橙色 | 当前选中 | 待下单 |

### 8.4 当前实际限制

普通用户只能购买：

- 开演前 30 分钟内的场次

如果下单失败，优先检查：

1. 场次是否距离开演太远
2. 座位是否已被别人锁定
3. 是否使用了错误账号

---

## 9. VIP 购票与退票流程

### 9.1 登录 VIP

```text
vip01 / 123456
```

可见菜单：

- 首页
- 购票
- 退票
- 个人中心

### 9.2 VIP 购票特点

VIP 可在：

- 开演前 50 分钟内

进行购票，比普通用户提前 20 分钟。

### 9.3 VIP 退票测试

当前前端允许 VIP 访问：

```text
/return-ticket
```

你可以直接测试：

1. 选择剧目
2. 选择排片
3. 选中已售出的座位
4. 提交退票流程

> 因为当前后端接口做了统一角色限制，VIP 在当前代码里是能走通相关订单操作的。

---

## 10. 售票员测试说明

### 10.1 登录售票员

```text
seller01 / 123456
```

可见菜单：

- 首页
- 退票
- 未处理订单
- 个人中心

### 10.2 当前真实情况

虽然售票员能进入：

- `/return-ticket`
- `/unhandled`

但后端订单相关接口当前只允许：

- VIP `4`
- 普通用户 `5`

因此你对售票员应这样理解：

#### 可以测的

- 登录是否正常
- 页面菜单显示是否正常
- 未处理订单页面是否能打开

#### 不建议当成“已打通”的能力

- 售票员提交退票订单
- 售票员提交购票订单

因为这部分很可能会被后端拦截。

---

## 11. 未处理订单

### 11.1 页面入口

账号：

```text
seller01 / 123456
```

路由：

```text
/unhandled
```

调用接口：

```text
GET /rest/analysis/selectCancelSaleByEmployeeId
```

用于查看当前员工关联的未完成/未处理订单数据。

---

## 12. 销售分析

### 12.1 登录经理

```text
manager / 123456
```

路由：

```text
/analysis
```

当前可用于验证：

- 总销售额
- 员工销售相关统计
- 影片票房排行
- 排片占比
- 趋势图显示

因为初始化数据里已经带有部分订单，所以不是空页面。

---

## 13. 个人中心与修改密码

所有已登录用户都可以访问：

```text
/profile
```

密码修改接口：

```text
POST /rest/employee/updatePassword
```

如果你改了密码，后续登录要用新密码。

建议测试完成后，如需保持统一测试口径，再重新执行数据库初始化脚本恢复默认账号密码。

---

## 14. 常见问题

### Q1：为什么登录账号和以前文档写的不一样？

因为当前 `init.sql` 已经换成了新的测试数据，旧演示账号已经不再使用。

当前请使用：

- `admin`
- `manager`
- `seller01`
- `vip01`
- `user01`

---

### Q2：为什么页面能打开，但按钮点了失败？

因为当前前端菜单权限与后端接口权限并不完全一致。

最典型例子：

- 售票员能进退票页
- 但后端未必允许其提交订单类操作

---

### Q3：为什么我买不了初始化场次？

因为当前后端对购票时间窗口有限制：

- 普通用户：开演前 30 分钟内
- VIP：开演前 50 分钟内

如果初始化场次距离现在太久，就不能立刻购买。

---

### Q4：改了 `init.sql` 但 Docker 数据没变化？

因为 MySQL 初始化脚本通常只在第一次建库时执行一次。

重置命令：

```bash
docker compose down -v
docker compose up -d --build
```

---

### Q5：中文乱码怎么排查？

优先检查：

1. `init.sql` 是否为 UTF-8 编码
2. MySQL 是否为 `utf8mb4`
3. 数据是否真的重新导入了
4. 浏览器是否还在看旧缓存页面

---

## 15. 推荐验收顺序

如果你现在要做一轮完整验收，建议按下面顺序：

1. `admin / admin123` 登录
2. 查看用户管理、演出厅管理
3. `manager / 123456` 登录
4. 查看字典、剧目、排片、座位、销售分析
5. 新增一个 30 分钟内开演的排片
6. `user01 / 123456` 登录并测试购票
7. 新增一个 30～50 分钟内开演的排片
8. `vip01 / 123456` 登录并测试提前购票
9. `vip01 / 123456` 测试退票页面
10. `seller01 / 123456` 登录检查未处理订单页和退票页展示
11. 最后确认中文、时间、金额、票状态显示都正常
